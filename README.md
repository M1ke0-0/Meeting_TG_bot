# 🤖 Telegram Bot для организации мероприятий

Чат-бот для Telegram, предназначенный для организации мероприятий, поиска участников и общения между пользователями по интересам.

## 📋 Описание

Бот позволяет пользователям:
- 👤 **Создавать профили** с указанием интересов, региона, возраста
- 💬 **Находить друзей** по общим интересам и географии
- 🎉 **Создавать мероприятия** и приглашать участников
- 📍 **Просматривать события на карте** с геолокацией
- 💌 **Общаться** с другими пользователями через встроенный чат

### Роли пользователей

**Пользователь** — может создавать профиль, искать друзей, создавать и участвовать в мероприятиях

**Администратор** — дополнительно может:
- Загружать списки регионов и интересов через Excel
- Выгружать отчеты по пользователям и мероприятиям
- Управлять данными бота
- Автоматически попадает в админ-панель после регистрации

## 🛠 Технологии

- **Python 3.12+**
- **aiogram 3.x** — асинхронный фреймворк для Telegram Bot API
- **PostgreSQL + asyncpg** — асинхронная работа с базой данных
- **SQLAlchemy 2.0** — ORM с поддержкой async/await
- **geopy** — геокодирование адресов через Nominatim API
- **openpyxl** — работа с Excel файлами
- **Docker** — контейнеризация приложения

## 📦 Установка

### 1. Клонируйте репозиторий

```bash
git clone <repository-url>
cd ANTY_TEST_BOT
```

### 2. Создайте виртуальное окружение

```bash
python3 -m venv venv
source venv/bin/activate  # для macOS/Linux
# или
venv\Scripts\activate  # для Windows
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения

Скопируйте файл `.env_sample` в `.env`:

```bash
cp .env_sample .env
```

Отредактируйте `.env`:

```env
BOT_TOKEN=your_bot_token_from_botfather
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/botdb
ADMIN_PHONES=+79001234567,+79007654321
```

### 5. Создайте базу данных PostgreSQL

```bash
createdb botdb
```

Или через Docker:

```bash
docker run -d --name postgres -e POSTGRES_DB=botdb -e POSTGRES_USER=user -e POSTGRES_PASSWORD=password -p 5432:5432 postgres:16
```

## 🚀 Запуск

### Локальный запуск

```bash
python3 run.py
```

При первом запуске автоматически создаются все таблицы в базе данных.

### Docker запуск

```bash
docker build -t meeting-bot .
docker run --env-file .env meeting-bot
```

## 📁 Структура проекта

```
ANTY_TEST_BOT/
├── run.py                      # Точка входа, запуск бота
├── config.py                   # Конфигурация (токены, пути)
├── Dockerfile                  # Конфигурация Docker
├── requirements.txt            # Зависимости Python
├── reset_db.py                 # Скрипт сброса БД
│
├── database/                   # Работа с базой данных
│   ├── __init__.py            # Экспорт модулей
│   ├── db_config.py           # Настройка движка SQLAlchemy
│   ├── session.py             # Асинхронные сессии
│   ├── models.py              # SQLAlchemy модели (User, Event, etc.)
│   └── repositories/          # Паттерн Repository
│       ├── base.py            # Базовый класс репозитория
│       ├── user.py            # Операции с пользователями
│       ├── event.py           # Операции с мероприятиями
│       ├── friend.py          # Друзья и заявки
│       ├── participant.py     # Участники мероприятий
│       ├── invite.py          # Приглашения
│       ├── region.py          # Регионы
│       └── interest.py        # Интересы
│
├── handlers/                   # Обработчики команд
│   ├── user.py                # Профиль, помощь, главное меню
│   ├── registration.py        # Регистрация и редактирование профиля
│   ├── admin.py               # Админ-панель
│   ├── events.py              # Создание и управление мероприятиями
│   └── communication.py       # Поиск друзей, чаты, заявки
│
├── keyboards/                  # Клавиатуры бота
│   └── builders.py            # Reply и Inline клавиатуры
│
├── middlewares/                # Middleware
│   └── user_middleware.py     # Загрузка данных пользователя, проверка роли
│
├── states/                     # FSM состояния
│   └── states.py              # Состояния регистрации, событий, поиска
│
├── utils/                      # Утилиты
│   ├── excel.py               # Экспорт отчетов в Excel
│   ├── geocoding.py           # Геокодирование адресов
│   └── validation.py          # Валидация телефонов, экранирование HTML
│
├── .env                        # Переменные окружения (не в Git)
├── .env_sample                 # Пример конфигурации
├── .gitignore                  # Игнорируемые файлы
└── README.md                   # Документация
```

## 🎯 Основные функции

### Для пользователей

#### 1. Регистрация
- Предоставление номера телефона через кнопку Telegram
- Заполнение профиля: имя, фамилия, пол, возраст
- Выбор региона и интересов (если загружены админом)
- Загрузка фото (JPG, PNG) и геолокации (опционально)
- Автоматическое определение роли админа после регистрации

#### 2. Мой профиль
- Просмотр данных профиля с фото
- Редактирование любых полей по отдельности
- Уведомление о незаполненных полях (если данные доступны)

#### 3. Общение
- **Друзья** — список друзей с возможностью написать или удалить
- **Входящие заявки** — принять или отклонить заявки в друзья
- **Поиск друзей** — фильтр по полу, региону, возрасту, интересам
- **Чат** — личные сообщения (текст, фото, видео, стикеры, голосовые)

#### 4. Мероприятия
- **Мероприятия друзей** — события от друзей с возможностью участия
- **Мои мероприятия** — организованные и те, где вы участник
- **Создать мероприятие**:
  - Название, дата (DD.MM.YYYY), время (HH:MM)
  - Выбор интересов, адрес с геокодированием
  - Описание и фото
  - Приглашение друзей при создании

#### 5. Карточка мероприятия
- Просмотр на карте (Telegram Venue)
- Список участников (с возможностью удаления организатором)
- Кнопки участия/отказа
- Приглашение друзей в существующее мероприятие

### Для администраторов

#### 1. Загрузка списков
- Excel файл с листами "Interests" и "Regions"
- Или один лист с двумя столбцами
- Автоматическое обновление данных в БД

#### 2. Отчеты
- **Отчет по пользователям** — все пользователи с контактами и профилями
- **Отчет по мероприятиям** — все события с данными организаторов и количеством участников

## 🔧 Настройка администраторов

Администраторы определяются по номеру телефона в файле `.env`:

```env
ADMIN_PHONES=+79001234567,+79007654321
```

Особенности:
- При регистрации пользователь с таким телефоном автоматически попадает в админ-панель
- Роль определяется при каждом запросе через middleware
- Телефоны нормализуются (удаляются пробелы, скобки, дефисы)

## 🗄 База данных

Используется PostgreSQL с асинхронным драйвером asyncpg. Основные таблицы:

| Таблица | Описание |
|---------|----------|
| `users` | Пользователи и их профили |
| `friends` | Двусторонние связи друзей |
| `friend_requests` | Заявки в друзья с message_id для удаления |
| `events` | Мероприятия с координатами |
| `event_participants` | Участники мероприятий |
| `event_invites` | Приглашения на мероприятия |
| `regions` | Список регионов (загружается админом) |
| `interests` | Список интересов (загружается админом) |

### Сброс базы данных

```bash
python3 reset_db.py
```

## 🌐 Геокодирование

Адреса мероприятий преобразуются в координаты через [Nominatim API](https://nominatim.openstreetmap.org/). Перед сохранением пользователь подтверждает найденный адрес.

## 📝 Логирование

Логи выводятся в консоль:
```
%(asctime)s | %(levelname)-8s | %(name)s → %(message)s
```

Предупреждения при старте:
- Пустая таблица `regions` — требуется загрузка через админ-панель
- Пустая таблица `interests` — требуется загрузка через админ-панель

## 🐛 Решение проблем

### SSL Certificate Error
```bash
/Applications/Python\ 3.*/Install\ Certificates.command
```

### Connection refused (PostgreSQL)
Убедитесь, что PostgreSQL запущен и доступен по указанному в `DATABASE_URL` адресу.

### Timeout при запуске
Проверьте стабильность интернет-соединения. При необходимости используйте VPN.

### Бот не отвечает
1. Проверьте токен в `.env`
2. Убедитесь, что бот запущен
3. Проверьте логи на ошибки
4. Убедитесь что PostgreSQL доступен

## 🐳 Docker Compose (опционально)

```yaml
version: '3.8'
services:
  bot:
    build: .
    env_file: .env
    depends_on:
      - db
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: botdb
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
```

## 👨‍💻 Разработка

Рекомендации:
1. Использовать виртуальное окружение
2. Следовать PEP 8 для стиля кода
3. Не коммитить `.env` в Git
4. Использовать `reset_db.py` для сброса БД при тестировании

---

**Автор:** Kirill  
**Версия:** 2.0.0  
**Дата:** 2026
