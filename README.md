# 🤖 Telegram Bot для организации мероприятий

Чат-бот для Telegram, предназначенный для организации мероприятий, поиска участников и общения между пользователями по интересам.

## 📋 Описание

Бот позволяет пользователям:
- 👤 **Создавать профили** с указанием интересов, региона, возраста
- 💬 **Находить друзей** по общим интересам и географии
- 🎉 **Создавать мероприятия** и приглашать участников
- 📍 **Просматривать события на карте** с геолокацией
- 💌 **Общаться** с другими пользователями через встроенный чат

### Роли пользователей

**Пользователь** - может создавать профиль, искать друзей, создавать и участвовать в мероприятиях

**Администратор** - дополнительно может:
- Загружать списки регионов и интересов через Excel
- Выгружать отчеты по пользователям и мероприятиям
- Управлять данными бота

## 🛠 Технологии

- **Python 3.12+**
- **aiogram 3.x** - асинхронный фреймворк для Telegram Bot API
- **SQLite** - база данных
- **aiohttp** - для геокодирования адресов через Nominatim API
- **openpyxl** - работа с Excel файлами

## 📦 Установка

### 1. Клонируйте репозиторий

```bash
git clone <repository-url>
cd ANTY_TEST_BOT
```

### 2. Создайте виртуальное окружение

```bash
python3 -m venv venv
source venv/bin/activate  # для macOS/Linux
# или
venv\Scripts\activate  # для Windows
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения

Скопируйте файл `.env_sample` в `.env`:

```bash
cp .env_sample .env
```

Отредактируйте `.env` и укажите:

```env
BOT_TOKEN=your_bot_token_from_botfather
ADMIN_PHONES=+79001234567,+79007654321
```

**Как получить токен бота:**
1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в `.env`

## 🚀 Запуск

```bash
python3 run.py
```

При первом запуске автоматически создастся база данных `bot.db` со всеми необходимыми таблицами.

## 📁 Структура проекта

```
ANTY_TEST_BOT/
├── run.py                  # Точка входа, запуск бота
├── config.py               # Конфигурация (токены, пути)
├── main.py                 # Основной файл (legacy)
├── database/               # Работа с базой данных
│   ├── core.py            # Инициализация БД, схемы таблиц
│   ├── users.py           # Операции с пользователями
│   ├── events.py          # Операции с мероприятиями
│   └── common.py          # Общие функции (регионы, интересы)
├── handlers/               # Обработчики команд и сообщений
│   ├── user.py            # Профиль, помощь
│   ├── registration.py    # Регистрация пользователей
│   ├── admin.py           # Админ-панель
│   ├── events.py          # Создание и управление мероприятиями
│   └── communication.py   # Поиск друзей, чаты
├── keyboards/              # Клавиатуры для бота
│   └── builders.py        # Генерация Reply и Inline клавиатур
├── middlewares/            # Middleware
│   └── user_middleware.py # Загрузка данных пользователя
├── states/                 # FSM состояния
│   └── states.py          # Определение состояний
├── utils/                  # Утилиты
│   ├── excel.py           # Экспорт отчетов в Excel
│   ├── geocoding.py       # Геокодирование адресов
│   └── validation.py      # Валидация и экранирование
├── .env                    # Переменные окружения (не в Git)
├── .env_sample             # Пример конфигурации
├── .gitignore              # Игнорируемые файлы
├── requirements.txt        # Зависимости Python
└── README.md               # Документация
```

## 🎯 Основные функции

### Для пользователей

#### 1. Регистрация
- Предоставление номера телефона
- Заполнение профиля: имя, фамилия, пол, возраст, регион, интересы
- Загрузка фото и геолокации (опционально)

#### 2. Мой профиль
- Просмотр данных профиля
- Редактирование любых полей

#### 3. Общение
- **Друзья** - список добавленных друзей, возможность начать чат
- **Поиск друзей** - поиск по интересам, региону, возрасту, полу
- **Чат** - личные сообщения между пользователями

#### 4. Мероприятия
- **Мероприятия друзей** - события от друзей, участие в них
- **Мои мероприятия** - созданные события и те, где вы участник
- **Создать мероприятие**:
  - Название, дата, время
  - Интересы, адрес (с геокодированием)
  - Описание и фото
  - Приглашение друзей

#### 5. Карточка мероприятия
- Просмотр на карте (Telegram Venue)
- Список участников
- Кнопки участия/отказа

### Для администраторов

#### 1. Загрузка списков
- Excel файл с двумя столбцами: Интересы и Регионы
- Автоматическое обновление данных в БД

#### 2. Отчеты
- **Отчет по пользователям** - Excel файл со всеми пользователями
- **Отчет по мероприятиям** - Excel файл со всеми событиями, включая:
  - Данные организатора (имя, фамилия, телефон)
  - Ссылки на изображения (Photo/Document File ID)
  - Количество участников

## 🔧 Настройка администраторов

Администраторы определяются по номеру телефона в файле `.env`:

```env
ADMIN_PHONES=+79001234567,+79007654321
```

При входе в бот с указанным номером пользователь получает доступ к админ-панели.

## 🗄 База данных

Используется SQLite (`bot.db`). Основные таблицы:

- `users` - пользователи и их профили
- `friends` - связи между друзьями
- `friend_requests` - запросы в друзья
- `events` - мероприятия
- `event_participants` - участники мероприятий
- `regions` - список регионов
- `interests` - список интересов

## 🌐 Геокодирование

Адреса мероприятий автоматически преобразуются в координаты с помощью [Nominatim API](https://nominatim.openstreetmap.org/) (OpenStreetMap). Координаты сохраняются в БД и используются для отображения на карте.

## 📝 Логирование

Логи выводятся в консоль с уровнем `INFO`. Формат:
```
%(asctime)s | %(levelname)-8s | %(name)s → %(message)s
```

## 🐛 Решение проблем

### SSL Certificate Error
Если возникает ошибка `SSL: CERTIFICATE_VERIFY_FAILED`:

```bash
/Applications/Python\ 3.*/Install\ Certificates.command
```

### База данных заблокирована
Если появляется ошибка "database is locked", убедитесь, что не запущено несколько экземпляров бота одновременно.

### Бот не отвечает
1. Проверьте правильность токена в `.env`
2. Убедитесь, что бот запущен (`python3 run.py`)
3. Проверьте логи на наличие ошибок

## 📄 Лицензия

Проект создан в учебных целях.

## 👨‍💻 Разработка

Для разработки рекомендуется:
1. Использовать виртуальное окружение
2. Следовать PEP 8 для стиля кода
3. Тестировать изменения перед коммитом
4. Не коммитить `.env` и `bot.db` в Git

---

**Автор:** Kirill  
**Версия:** 1.0.0  
**Дата:** 2026
